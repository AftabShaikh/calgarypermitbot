[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "calgarypermitbot"
version = "1.0.0"
description = "Calgary Permit Bot"
dependencies = []

[tool.setuptools]
py-modules = []